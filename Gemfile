source 'https://rubygems.org'

# Use the GitHub Pages gem to match GitHub Pages' build environment.
# Leaving the version unspecified will install the latest `github-pages`
# compatible with the current Bundler environment when you run `bundle install`.
gem 'github-pages', group: :jekyll_plugins

# If you rely on a local theme or specific plugins you can add them here.
# The `github-pages` gem provides the common Pages theme(s) and plugins.
